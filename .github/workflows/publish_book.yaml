name: Publish Book of Ryan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions: write-all
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v2

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v1
        with:
          mdbook-version: 'latest'

      - name: MdBook build
        run: |
          cd book-of-ryan
          mdbook build
          cp -r ./book /home/runner/work/book

      - name: Checkout GitHub Pages repo
        uses: actions/checkout@v2
        with:
          repository: tokahuke/tokahuke.github.io
      
      - name: Copy artifacts into GitHub Pages repo
        run: |
          mkdir -p ./book-of-ryan/book
          cp -r /home/runner/work/book ./book-of-ryan/book
      
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./book-of-ryan/book
          output-dir: ./book-of-ryan
